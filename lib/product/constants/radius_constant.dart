import 'package:flutter/material.dart';

class BorderStyles extends Border {
  BorderRadius get borderInput => const BorderRadius.all(Radius.circular(20.0));
  BorderRadius get borderCard => const BorderRadius.all(Radius.circular(15.0));
}
